let x = [];
x.length = 3;
let y = [undefined, undefined, undefined];

console.log(`The length of Object.keys(x) is ${Object.keys(x).length}.`);
console.log(`The length of Object.keys(y) is ${Object.keys(y).length}.`);
